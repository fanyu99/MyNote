# YOLO 学习进度记忆

> 建立日期：2026年9月18日
> 学习者：PyTorch 初学者，正在从图像分类过渡到目标检测

## 一、当前基础

- 已学习 PyTorch 基础知识。
- 已使用 CIFAR-10 数据集完成一个简单图像分类模型的训练。
- 当前测试准确率：约 73%。
- 下一步学习目标：理解并实践 YOLO 目标检测算法。

## 二、第一课开始前的代码判断

本文件将在第一课开始时根据以下代码进行学习进度判断：

- `D:\python\CIFAR10\model.py`
- `D:\python\CIFAR10\train.py`

判断重点包括：

- 是否理解 `Dataset`、`DataLoader` 和批量训练流程；
- 是否理解模型、损失函数、优化器和反向传播；
- 是否能够区分图像分类与目标检测的输入、输出和标签；
- 是否具备阅读 YOLO 训练代码的基础。

## 三、YOLO 学习路线

### 阶段 1：从图像分类过渡到目标检测

- [ ] 理解图像分类、目标检测、语义分割和实例分割的区别
- [ ] 理解目标检测标签：类别、边界框、置信度
- [ ] 理解 IoU、Precision、Recall、AP 和 mAP
- [ ] 理解 YOLO 的基本思想：一次前向传播完成检测

### 阶段 2：YOLO 基础结构

- [ ] 理解 Backbone、Neck、Head
- [ ] 理解多尺度特征和特征图
- [ ] 理解检测头输出张量
- [ ] 理解边界框回归和类别预测
- [ ] 理解 NMS（非极大值抑制）

### 阶段 3：使用 YOLO 完成实践

- [ ] 安装并运行一个 YOLO 项目
- [ ] 使用预训练模型进行图片推理
- [ ] 使用摄像头或视频进行推理
- [ ] 准备自定义数据集
- [ ] 标注并转换数据集格式
- [ ] 训练自定义目标检测模型
- [ ] 评估并改进模型

### 阶段 4：深入理解与改进

- [ ] 阅读 YOLO 核心代码
- [ ] 理解数据增强
- [ ] 理解迁移学习
- [ ] 理解训练超参数
- [ ] 分析检测错误
- [ ] 尝试改进模型结构或训练策略

## 四、课程记录

### 第 1 课：从 CIFAR-10 图像分类进入 YOLO 目标检测

- 日期：2026年9月18日
- 状态：进行中
- 本课目标：建立图像分类与目标检测的概念联系，理解 YOLO 要解决的问题。
- 学习内容：待补充
- 实践任务：待补充
- 遇到的问题：待补充
- 课后总结：待补充

## 五、学习原则

1. 先理解任务差异，再学习 YOLO 网络结构。
2. 先能够运行和观察结果，再逐步阅读源码。
3. 每次学习都保留实验结果，而不是只记理论。
4. 将每个 YOLO 概念和已经学过的 PyTorch 概念建立联系。

## 六、后续更新规则

每完成一节课，更新以下内容：

- 完成的知识点；
- 实际运行的代码；
- 实验数据和可视化结果；
- 当前仍然不理解的概念；
- 下一节课的目标。

## 七、第一课学习记录：从 CIFAR-10 图像分类进入目标检测

### 1. 代码学习进度判断

根据 `D:\python\CIFAR10\model.py` 和 `D:\python\CIFAR10\train.py` 判断：

- 已经能够使用 `nn.Module` 和 `nn.Sequential` 搭建卷积神经网络；
- 理解卷积、BatchNorm、激活函数、池化、Flatten 和全连接层的基本作用；
- 能够根据 CIFAR-10 的输入尺寸 32×32 推导卷积网络的空间尺寸变化；
- 理解 `Dataset`、`DataLoader`、batch、epoch、前向传播、损失计算、反向传播和优化器更新；
- 能够使用 `CrossEntropyLoss` 完成十分类任务；
- 能够完成训练集训练、测试集评估和模型参数保存。

当前阶段判断：**已经具备学习目标检测训练流程的基础，但还需要建立检测标签、边界框、IoU 和检测指标的概念。**

### 2. 代码中需要留意的问题

- `model.py` 最终输出形状是 `[batch_size, 10]`，表示每张图片对应 10 个类别的分类 logits；
- `train.py` 中 `CrossEntropyLoss` 接收的是类别索引标签，而不是边界框标签；
- 测试损失使用 `total_loss / test_datasize` 并不严谨，因为 `CrossEntropyLoss` 默认返回每个 batch 的平均损失，应该按样本数加权或直接对所有样本重新统计；
- `batchsize=1000` 较大，后续学习检测时需要根据显存和图片尺寸调整；
- 当前数据预处理只有 `ToTensor()`，还没有使用标准化和数据增强；这不是 YOLO 入门的阻碍，但后续会学习。

### 3. 本课已学习知识

- 图像分类回答：“这张图是什么？”
- 目标检测回答：“图中有什么、在哪里、可信度是多少？”
- 分类模型通常输出固定大小的 `[B, C]` 张量；检测模型需要为一张图片预测数量可变的多个目标；
- 一个目标检测标签至少包含：类别 `class`、边界框中心坐标 `x_center/y_center`、边界框宽高 `width/height`；
- YOLO 格式常用归一化后的 `class x_center y_center width height`，坐标范围通常相对于图片宽高归一化到 0～1，类别编号从 0 开始；
- YOLO 与 CIFAR-10 训练仍然共享“数据集—模型—损失—反向传播—优化器”的基本训练框架，但标签、模型输出、损失函数和评价指标发生了变化；
- YOLO 的核心直觉是用一次网络前向传播直接预测目标的位置和类别，而不是先单独提出候选区域再分类。

### 4. 本课练习

- [ ] 能够说明分类任务和目标检测任务的输入、输出差异
- [ ] 能够解释一行 YOLO 标签中五个数字的含义
- [ ] 能够根据一个像素坐标边界框计算归一化 `xywh`
- [ ] 能够解释为什么检测模型不能简单地使用 `[B, 10]` 作为输出

### 5. 第一课状态

- 状态：已开始
- 理论掌握情况：待学习者回答练习题后确认
- 下一课：边界框坐标、IoU、Precision、Recall、AP 和 mAP

### 第一课练习题反馈

- 问题 1：回答正确。`10` 表示 CIFAR-10 的 10 个类别，模型为每张图片输出 10 个类别分数。
- 问题 2：回答正确。目标检测在分类的基础上，还要确定目标在图像中的位置。
- 问题 3：回答正确。`2` 是类别编号，后四个数是归一化后的中心点坐标和边界框宽高。
- 问题 4：计算正确。像素格式为 `(192, 320, 256, 384)`，归一化后的 `xywh` 为 `(0.3, 0.5, 0.4, 0.6)`。
- 问题 5：方向基本正确。仅修改最后一层不能完成目标检测，因为检测还需要预测边界框位置、目标置信度，并处理一张图片中的多个目标；同时还需要适配检测标签、定位损失、解码过程和 NMS 等后处理。

第一课练习结果：**5/5，基础概念已掌握**。

已完成知识点：

- [x] 能够说明分类任务和目标检测任务的输入、输出差异
- [x] 能够解释一行 YOLO 标签中五个数字的含义
- [x] 能够根据像素坐标边界框计算归一化 `xywh`
- [x] 能够解释为什么检测模型不能简单地使用 `[B, 10]` 作为输出

下一课：边界框坐标、IoU、Precision、Recall、AP 和 mAP。

## 八、第二课：边界框、IoU、Precision、Recall、AP 和 mAP

- 日期：2026年9月18日
- 状态：进行中
- 开始原因：第一课练习 5/5，已具备进入目标检测评价指标的基础。
- 本课目标：理解模型预测框如何与真实框匹配，以及如何评价检测结果。
- 学习内容：待完成
- 练习结果：待完成

### 第二课练习题反馈

- 问题 1：回答正确。交集面积为 `25`，并集面积为 `175`，IoU 为 `25/175 ≈ 0.143`，可写作约 `0.14`。
- 问题 2：回答正确。`TP=2`、`FP=2`、`FN=1`，`Precision=2/(2+2)=0.5`，`Recall=2/(2+1)≈0.67`。
- 问题 3：回答正确。类别虽然正确，但 IoU 为 `0.4`，低于 `0.5` 阈值，因此该预测不能计为 TP；在评估匹配中通常计为 FP，同时对应真实目标会形成 FN。
- 问题 4：回答正确。说明模型大致能找到目标，但边界框定位不够精确，在更严格的 IoU 阈值下表现明显下降。
- 问题 5：回答正确。降低置信度阈值通常会保留更多预测，Recall 可能提高，但误报也可能增加，Precision 可能下降；实际变化仍取决于模型和数据集。

第二课练习结果：**5/5，已掌握 IoU、TP、FP、FN、Precision、Recall 和 mAP 指标的基本含义**。

已完成知识点：

- [x] 能够计算交集面积、并集面积和 IoU
- [x] 能够计算 Precision 和 Recall
- [x] 能够根据类别正确性和 IoU 阈值判断预测是否合格
- [x] 能够解释 mAP@0.5 与 mAP@0.5:0.95 的差异
- [x] 能够解释降低置信度阈值对 Precision 和 Recall 的通常影响

第二课状态：已完成。
下一课：YOLO 的整体网络结构——Backbone、Neck、Head。

### 第三课练习题反馈

- 问题 1：回答正确。Backbone 负责从输入图像中提取不同层次、不同语义深度的特征。
- 问题 2：回答正确。不同大小的目标需要不同尺度的特征图；小目标通常依赖空间分辨率更高的浅层特征，大目标通常更依赖语义信息更丰富的深层特征。
- 问题 3：回答正确。Neck 负责融合不同尺度、不同层次的特征。
- 问题 4：回答正确。Head 负责预测边界框位置、目标置信度和类别。
- 问题 5：计算方式正确。输出数量为 `4 + 1 + 20 = 25`。
- 问题 6：回答正确。Backbone 提取特征，Neck 融合特征，Head 根据融合后的特征完成检测预测。

第三课练习结果：**6/6，已掌握 YOLO Backbone、Neck、Head 的基本职责**。

已完成知识点：

- [x] 理解 Backbone 的作用
- [x] 理解多尺度特征对小目标和大目标检测的意义
- [x] 理解 Neck 的特征融合职责
- [x] 理解 Head 的检测输出内容
- [x] 能够计算单个预测位置的输出维度
- [x] 能够概括 Backbone、Neck、Head 的完整关系

第三课状态：已完成。

## 九、学习暂停记录

- 暂停日期：2026年9月18日
- 已完成课程：第一课、第二课、第三课
- 当前掌握情况：已掌握从 CIFAR-10 图像分类过渡到 YOLO 目标检测所需的基础概念，包括检测标签、边界框、IoU、TP/FP/FN、Precision、Recall、AP、mAP、Backbone、Neck 和 Head。
- 当前未开始内容：YOLO 检测输出的详细解码、边界框预测参数、置信度筛选和 NMS 的实现原理。
- 下次学习起点：第四课——YOLO 的检测输出、边界框解码和 NMS 实现原理。
- 备注：本次学习到第三课结束，等待下一次继续。
